name: RocketAPI
version: '${project.version}'
main: api.sseakk.rocketapi.RocketAPI
api-version: 1.20
prefix: RocketAPI
load: STARTUP
authors: [ sSeaKk ]
description: Internal API of sSeaKk

commands:
  rocketapi:
    description: "Api command."
    usage: "/rapi [version/author]"
    aliases: [ rapi, rocketapi ]
    permission: rocketapi.use
    permission-message: "You do not have permission to use this plugin."